# Shell scripts and Docker entrypoints must keep LF endings so they run on Linux
# even when the repo is cloned/edited on Windows.
*.sh            text eol=lf
entrypoint*     text eol=lf
Dockerfile*     text eol=lf
*.ps1           text eol=crlf
